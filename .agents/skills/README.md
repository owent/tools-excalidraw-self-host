# Agent Skills

Shared project skills live under `.agents/skills/`.

| Skill | Use when |
| --- | --- |
| `excalidraw-pages-deploy` | Maintaining the Excalidraw GitHub Pages workflow, upstream build patching, custom font list, domain setup docs, or weekly release automation. |

Keep this index concise. Read a skill body only when the task matches its `description`.
