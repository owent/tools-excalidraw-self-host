@AGENTS.md

## Claude Code

- Use `AGENTS.md` as the shared project instruction source.
- Add Claude-specific behavior here only when verified current Claude Code behavior cannot be expressed in `AGENTS.md`.
